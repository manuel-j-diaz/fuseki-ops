name: Build and publish Jena CLI image

on:
  push:
    branches: [main]
    paths:
      - "jena/**"
      - ".github/workflows/jena.yaml"
    tags: ["jena/v*"]
  pull_request:
    branches: [main]
    paths:
      - "jena/**"
      - ".github/workflows/jena.yaml"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/jena

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=match,pattern=jena/v(\d+\.\d+\.\d+),group=1
            type=match,pattern=jena/v(\d+\.\d+),group=1
            type=sha,prefix=

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: jena
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: JENA_VERSION=6.0.0
          cache-from: type=gha
          cache-to: type=gha,mode=max
